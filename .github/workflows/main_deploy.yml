# Pipeline CI/CD pour déployer automatiquement le code Salesforce
# Utilise sfdx-git-delta pour déployer uniquement les fichiers modifiés
# Se déclenche sur push ou pull request vers la branche main

name: Deploy and Validate Metadata

# Déclencheurs : le pipeline s'exécute sur push ou pull request vers main
on:
  pull_request:
    branches:
      - main

  push:
    branches:
      - main

# Définit les différents jobs (tâches) qui composent ce workflow
jobs:
  # Job pour valider et déployer les métadonnées
  sfdxvalidate:
    name: "Run SFDX Validate and Deploy"
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]

    steps:
      # Étape 1 : Récupère le code depuis GitHub
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Nécessaire pour sfdx-git-delta qui compare les commits

      # Étape 2 : Configure Node.js
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      # Étape 3 : Installe Salesforce CLI et le plugin sfdx-git-delta
      # sfdx-git-delta permet de déployer uniquement les fichiers modifiés (delta)
      - name: Install SFDX CLI and sfdx-git-delta plugin
        run: |
          echo "📦 Installation de Salesforce CLI..."
          npm install -g @salesforce/cli@latest
          
          echo "📦 Installation de sfdx-git-delta..."
          # Installe sfdx-git-delta comme plugin Salesforce CLI
          sf plugins install sfdx-git-delta@latest || {
            echo "⚠️ Installation via plugin échouée, tentative via npm..."
            npm install -g sfdx-git-delta@latest || {
              echo "❌ Impossible d'installer sfdx-git-delta"
              exit 1
            }
          }
          
          echo "✅ Installation terminée"
          # Vérifie que la commande est disponible
          sf sgd --version || sfdx sgd --version || echo "⚠️ Commande sgd non disponible, déploiement complet sera utilisé"

      # Étape 4 : Authentification à Salesforce
      # Utilise les secrets GitHub pour se connecter à Salesforce
      - name: Authentification Salesforce
        env:
          SF_USERNAME: ${{ secrets.SF_USERNAME }}
          SF_PASSWORD: ${{ secrets.SF_PASSWORD }}
          SF_SECURITY_TOKEN: ${{ secrets.SF_SECURITY_TOKEN }}
          SF_INSTANCE_URL: ${{ secrets.SF_INSTANCE_URL }}
          SF_CONSUMER_KEY: ${{ secrets.SF_CONSUMER_KEY }}
          SF_CONSUMER_SECRET: ${{ secrets.SF_CONSUMER_SECRET }}
        run: |
          # Vérifie que les secrets sont configurés
          if [ -z "$SF_USERNAME" ] || [ -z "$SF_PASSWORD" ] || [ -z "$SF_SECURITY_TOKEN" ]; then
            echo "❌ Erreur: Les secrets SF_USERNAME, SF_PASSWORD ou SF_SECURITY_TOKEN ne sont pas configurés"
            exit 1
          fi
          
          echo "🔐 Authentification à Salesforce..."
          
          # NETTOYAGE COMPLET
          rm -rf ~/.sf ~/.sfdx ~/.local/share/sf ~/.config/sf 2>/dev/null || true
          
          # Combine password + security token
          FULL_PASSWORD="${SF_PASSWORD}${SF_SECURITY_TOKEN}"
          
          # Détermine l'URL d'instance
          # Pour les orgs personnalisées, utilise l'URL fournie, sinon login.salesforce.com
          if [ -n "$SF_INSTANCE_URL" ]; then
            INSTANCE_URL="$SF_INSTANCE_URL"
          else
            INSTANCE_URL="login.salesforce.com"
          fi
          # Nettoie l'URL
          INSTANCE_URL=$(echo "$INSTANCE_URL" | sed 's|^https://||' | sed 's|^http://||' | sed 's|/$||')
          LOGIN_URL="https://${INSTANCE_URL}"
          
          echo "📍 Instance URL utilisée: $LOGIN_URL"
          
          echo "🔗 Authentification OAuth2..."
          
          # Utilise Consumer Key/Secret si fournis, sinon essaie le Consumer Key par défaut
          if [ -n "$SF_CONSUMER_KEY" ] && [ -n "$SF_CONSUMER_SECRET" ]; then
            CLIENT_ID="$SF_CONSUMER_KEY"
            CLIENT_SECRET="$SF_CONSUMER_SECRET"
            echo "📋 Utilisation du Connected App personnalisé"
          else
            CLIENT_ID="3MVG9fTLmJ60pJ5LyUYh88ekgGn2hs2GtmS_7qWX8Z6f8Y5Z5Z5Z5Z5Z5Z5Z5Z5Z5Z"
            CLIENT_SECRET="PlatformCLI"
            echo "📋 Utilisation du Consumer Key par défaut (peut ne pas fonctionner)"
          fi
          
          # Nettoie les secrets (enlève espaces/retours à la ligne)
          CLIENT_ID=$(echo "$CLIENT_ID" | tr -d '[:space:]')
          CLIENT_SECRET=$(echo "$CLIENT_SECRET" | tr -d '[:space:]')
          
          echo "🔍 Vérification des paramètres..."
          echo "   Instance URL: $LOGIN_URL"
          echo "   Username: ${SF_USERNAME}"
          echo "   Consumer Key: ${CLIENT_ID:0:20}..." # Affiche seulement les 20 premiers caractères
          
          TOKEN_RESPONSE=$(curl -s -X POST "${LOGIN_URL}/services/oauth2/token" \
            -H "Content-Type: application/x-www-form-urlencoded" \
            -d "grant_type=password" \
            -d "client_id=${CLIENT_ID}" \
            -d "client_secret=${CLIENT_SECRET}" \
            -d "username=${SF_USERNAME}" \
            -d "password=${FULL_PASSWORD}" 2>&1)
          
          if echo "$TOKEN_RESPONSE" | grep -q '"access_token"'; then
            ACCESS_TOKEN=$(echo "$TOKEN_RESPONSE" | sed -n 's/.*"access_token":"\([^"]*\)".*/\1/p')
            INSTANCE_URL_FROM_TOKEN=$(echo "$TOKEN_RESPONSE" | sed -n 's/.*"instance_url":"\([^"]*\)".*/\1/p')
            
            [ -z "$ACCESS_TOKEN" ] && { echo "❌ Impossible d'extraire access_token"; echo "$TOKEN_RESPONSE"; exit 1; }
            
            echo "✅ Token obtenu"
            sf org login access-token \
              --instance-url "${INSTANCE_URL_FROM_TOKEN:-$LOGIN_URL}" \
              --access-token "$ACCESS_TOKEN" \
              --alias deploy-org \
              --set-default || exit 1
          else
            echo "❌ Échec OAuth2"
            echo "Réponse complète: $TOKEN_RESPONSE"
            echo ""
            ERROR_TYPE=$(echo "$TOKEN_RESPONSE" | grep -o '"error":"[^"]*' | cut -d'"' -f4)
            if [ "$ERROR_TYPE" = "invalid_grant" ]; then
              echo "🔍 Erreur 'invalid_grant' = problème d'authentification"
              echo "   Vérifiez:"
              echo "   1. Username et Password sont corrects dans GitHub Secrets"
              echo "   2. Security Token est correct et à jour"
              echo "   3. Connected App a 'Enable Authorization Code and Credentials Flow' activé"
              echo "   4. Connected App a les scopes: api, refresh_token, offline_access"
            fi
            exit 1
          fi
          
          echo "✅ Authentification réussie"
          sf org display --target-org deploy-org

      # Étape 5 : Génère le delta des métadonnées modifiées
      # Compare les fichiers entre la branche actuelle et main
      # Génère un package.xml avec uniquement les fichiers modifiés
      - name: Generate metadata delta
        id: delta
        continue-on-error: true
        run: |
          # Crée un répertoire temporaire pour le package.xml
          mkdir -p .temp/package
          
          # Pour les pull requests, compare avec la branche de base
          # Pour les pushes sur main, compare avec le commit précédent
          if [ "${{ github.event_name }}" == "pull_request" ]; then
            BASE_SHA="${{ github.event.pull_request.base.sha }}"
            HEAD_SHA="${{ github.event.pull_request.head.sha }}"
          else
            # Pour un push, compare avec le commit précédent (ou origin/main si c'est le premier commit)
            BASE_SHA=$(git rev-parse HEAD~1 2>/dev/null || git rev-parse origin/main 2>/dev/null || echo "")
            HEAD_SHA="${{ github.sha }}"
          fi
          
          if [ -z "$BASE_SHA" ]; then
            echo "⚠️ Pas de commit précédent, déploiement de tout le projet"
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "use_full_deploy=true" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          echo "🔍 Comparaison: $BASE_SHA -> $HEAD_SHA"
          
          # Génère le package.xml avec sfdx-git-delta
          # Essaie d'abord avec la commande sf sgd
          if sf sgd:source:delta --to "$HEAD_SHA" --from "$BASE_SHA" --output .temp/package --generate-delta 2>/dev/null; then
            echo "✅ Delta généré avec succès"
          else
            echo "⚠️ Erreur avec sf sgd, tentative avec sfdx sgd..."
            # Alternative: essayer avec sfdx si sf ne fonctionne pas
            if command -v sfdx &> /dev/null; then
              sfdx sgd:source:delta --to "$HEAD_SHA" --from "$BASE_SHA" --output .temp/package --generate-delta || {
                echo "⚠️ Erreur lors de la génération du delta, déploiement complet"
                echo "has_changes=true" >> $GITHUB_OUTPUT
                echo "use_full_deploy=true" >> $GITHUB_OUTPUT
                exit 0
              }
            else
              echo "⚠️ sfdx-git-delta non disponible, déploiement complet"
              echo "has_changes=true" >> $GITHUB_OUTPUT
              echo "use_full_deploy=true" >> $GITHUB_OUTPUT
              exit 0
            fi
          fi
          
          # Vérifie si des fichiers ont été modifiés
          if [ -f .temp/package/package.xml ] && [ -s .temp/package/package.xml ]; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "use_full_deploy=false" >> $GITHUB_OUTPUT
            echo "✅ Métadonnées modifiées détectées"
            echo "📦 Contenu du package.xml:"
            cat .temp/package/package.xml
          else
            echo "has_changes=false" >> $GITHUB_OUTPUT
            echo "use_full_deploy=false" >> $GITHUB_OUTPUT
            echo "⚠️ Aucune métadonnée modifiée"
          fi

      # Étape 6 : Valide les métadonnées (sans déployer)
      # S'exécute toujours pour vérifier la syntaxe
      - name: Validate metadata
        if: steps.delta.outputs.has_changes == 'true'
        run: |
          # Valide les métadonnées sans les déployer
          # Utilise le répertoire source généré par sfdx-git-delta
          if [ -f .temp/package/package.xml ]; then
            echo "✅ Validation des métadonnées..."
            sf project deploy validate \
              --source-dir .temp/package \
              --target-org deploy-org \
              --wait 10 || {
                echo "⚠️ Erreur de validation (peut être normal si pas de métadonnées)"
                exit 0
              }
          else
            echo "⚠️ Aucun package.xml trouvé, validation ignorée"
          fi

      # Étape 7 : Exécute les tests (pour les pull requests)
      # Vérifie que les tests passent avant de merger
      - name: Run Apex Tests
        if: github.event_name == 'pull_request' && steps.delta.outputs.has_changes == 'true'
        run: |
          # Exécute tous les tests locaux
          sf apex run test \
            --test-level RunLocalTests \
            --code-coverage \
            --result-format human \
            --wait 10 \
            --target-org deploy-org || true

      # Étape 8 : Déploie les métadonnées sur main
      # Seulement si c'est un push sur main (pas une pull request)
      - name: Déployer les métadonnées sur la branch main
        if: github.ref == 'refs/heads/main' && github.event_name == 'push' && steps.delta.outputs.has_changes == 'true'
        run: |
          # Vérifie si on doit déployer tout le projet ou juste le delta
          if [ "${{ steps.delta.outputs.use_full_deploy }}" == "true" ] || [ ! -f .temp/package/package.xml ]; then
            echo "🚀 Déploiement complet du projet..."
            sf project deploy start \
              --source-dir force-app \
              --target-org deploy-org \
              --test-level RunLocalTests \
              --wait 10
          else
            echo "🚀 Déploiement des métadonnées modifiées (delta)..."
            sf project deploy start \
              --source-dir .temp/package \
              --target-org deploy-org \
              --test-level RunLocalTests \
              --wait 10
          fi
          
          # Vérifie le rapport de déploiement
          echo "📊 Rapport de déploiement:"
          sf project deploy report \
            --target-org deploy-org

      # Étape 9 : Génère un rapport de couverture de code (s'exécute toujours)
      - name: Generate Test Coverage Report
        if: always() && steps.delta.outputs.has_changes == 'true'
        run: |
          # Récupère le rapport de test, même si les tests ont échoué
          sf apex get test \
            --code-coverage \
            --result-format human \
            --target-org deploy-org || true
